# Local Toolbox 0.5.0 integration base

The 0.5.0 release candidate was integrated into `codex/0.5.0` from PR #7 after product CI passed.

Canonical integration commit:

`00a9604f265670238500564df55a6c08b8d33699`

Parallel QA/development tracks must branch from this exact commit unless a later integration checkpoint is explicitly published.

Production `latest.json` and `releases/` remain untouched until final human approval and Windows validation.
